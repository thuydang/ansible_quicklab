'''CLEAN UP:
    Setup VLANs and Bridges for kvm networks.
'''
---
- hosts: localhost
  tasks:
  - name: ensure apache is running
    service: 
      name: httpd
      state: started
  handlers:
    - name: restart apache
      service: name=httpd state=restarted

